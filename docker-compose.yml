version: '3'

services:
  # db:
  #   image: mysql:5
  #   restart: always
  #   ports:
  #     - 3307:3306
  #   environment:
  #     MYSQL_ROOT_PASSWORD: r00t
  #   volumes:
  #     - ./db:/var/lib/mysql
  shop:
    # build: shop
    image: prestashop/prestashop:1.7.6.9
    volumes:
      - ./shop:/var/www/html
    restart: always
    ports:
      - 80:80
    # environment:
    #   PS_INSTALL_AUTO: 1
    #   DB_SERVER: db
    # depends_on:
    #   - db
